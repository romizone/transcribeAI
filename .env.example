# TranscribeAI - Environment Variables
# No API key needed! 100% local transcription.

# Optional: Set model size (tiny, base, small, medium, large-v3)
# Default: small
# TRANSCRIBE_MODEL=small

# Optional: Set number of CPU threads
# Default: auto (all cores)
# TRANSCRIBE_THREADS=4
